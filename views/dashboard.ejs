<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/custom.css">
</head>
<body class="bg-light">
    <!-- Header -->
    <%- include('partials/page-header', { 
        title: 'Admin Dashboard',
        icon: 'bi-speedometer2',
        isDashboard: true,
        userName: userName,
        userRole: userRole
    }) %>

    <div class="container my-3">
        <!-- Welcome Section -->
        <div class="alert alert-light border-0 shadow-sm mb-2 py-2 px-3 animate-fade-in-up" style="background: linear-gradient(135deg, #f8f9fa 0%, #ffffff 100%);">
            <div class="d-flex align-items-center justify-content-between flex-wrap">
                <div>
                    <strong class="h6 mb-0 d-inline">Welcome back, <span class="text-gradient"><%= userName %></span>! 👋</strong>
                    <% if (userRole === 'associate' || userRole === 'team_leader') { %>
                        <span class="text-muted ms-2 small d-none d-md-inline">View your assigned partners' bookings and payments.</span>
                    <% } else { %>
                        <span class="text-muted ms-2 small d-none d-md-inline">Here's what's happening with your real estate business today.</span>
                    <% } %>
                </div>
            </div>
        </div>

        <% if (userRole !== 'associate' && userRole !== 'team_leader') { %>
        <!-- Stats Cards Row 1 -->
        <div class="row g-3 mb-3">
            <div class="col-6 col-md-4">
                <div class="card border-0 shadow-sm border-start border-primary border-4 stat-card animate-fade-in-up" style="animation-delay: 0.1s">
                    <div class="card-body text-center">
                        <i class="bi bi-grid-3x3-gap fs-1 text-primary"></i>
                        <h3 class="h2 fw-bold mt-2 mb-1"><%= stats.totalProjects %></h3>
                        <p class="text-muted small mb-0">Total Projects</p>
                    </div>
                </div>
            </div>
            <div class="col-6 col-md-4">
                <div class="card border-0 shadow-sm border-start border-success border-4 stat-card animate-fade-in-up" style="animation-delay: 0.2s">
                    <div class="card-body text-center">
                        <i class="bi bi-calendar-check fs-1 text-success"></i>
                        <h3 class="h2 fw-bold mt-2 mb-1"><%= stats.activeBookings %></h3>
                        <p class="text-muted small mb-0">Active Bookings</p>
                    </div>
                </div>
            </div>
            <div class="col-6 col-md-4">
                <div class="card border-0 shadow-sm border-start border-info border-4 stat-card animate-fade-in-up" style="animation-delay: 0.3s">
                    <div class="card-body text-center">
                        <i class="bi bi-people fs-1 text-info"></i>
                        <h3 class="h2 fw-bold mt-2 mb-1"><%= stats.activeCustomers %></h3>
                        <p class="text-muted small mb-0">Active Customers</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Stats Cards Row 2 - Financial -->
        <div class="row g-3 mb-4">
            <div class="col-12 col-md-6 col-lg-3">
                <div class="card border-0 shadow-sm border-start border-warning border-4 stat-card animate-fade-in-up" style="animation-delay: 0.4s">
                    <div class="card-body text-center">
                        <i class="bi bi-file-earmark-check-fill fs-1 text-warning"></i>
                        <h3 class="h2 fw-bold mt-2 mb-1">₹<%= parseFloat(stats.totalRevenue).toLocaleString('en-IN') %></h3>
                        <p class="text-muted small mb-0"><strong>Registered Property Revenue</strong></p>
                        <small class="text-muted" style="font-size: 0.75rem;"><i class="bi bi-info-circle"></i> Booking value of registered properties</small>
                    </div>
                </div>
            </div>
            <div class="col-12 col-md-6 col-lg-3">
                <div class="card border-0 shadow-sm border-start border-info border-4 stat-card animate-fade-in-up" style="animation-delay: 0.5s">
                    <div class="card-body text-center">
                        <i class="bi bi-calculator fs-1 text-info"></i>
                        <h3 class="h2 fw-bold mt-2 mb-1">₹<%= parseFloat(stats.totalRevenueAllBookings).toLocaleString('en-IN') %></h3>
                        <p class="text-muted small mb-0"><strong>Total Revenue (All Bookings)</strong></p>
                        <small class="text-muted" style="font-size: 0.75rem;"><i class="bi bi-check-circle-fill"></i> Registered + non-registered</small>
                    </div>
                </div>
            </div>
            <div class="col-12 col-md-6 col-lg-3">
                <div class="card border-0 shadow-sm border-start border-success border-4 stat-card animate-fade-in-up" style="animation-delay: 0.6s">
                    <div class="card-body text-center">
                        <i class="bi bi-cash-stack fs-1 text-success"></i>
                        <h3 class="h2 fw-bold mt-2 mb-1">₹<%= parseFloat(stats.totalPaymentsReceived).toLocaleString('en-IN') %></h3>
                        <p class="text-muted small mb-0"><strong>Payments Received</strong></p>
                        <small class="text-muted" style="font-size: 0.75rem;"><i class="bi bi-wallet2"></i> Actual cash collected</small>
                    </div>
                </div>
            </div>
            <div class="col-12 col-md-6 col-lg-3">
                <div class="card border-0 shadow-sm border-start border-danger border-4 stat-card animate-fade-in-up" style="animation-delay: 0.7s">
                    <div class="card-body text-center">
                        <i class="bi bi-hourglass-split fs-1 text-danger"></i>
                        <h3 class="h2 fw-bold mt-2 mb-1">₹<%= parseFloat(stats.pendingRevenue).toLocaleString('en-IN') %></h3>
                        <p class="text-muted small mb-0"><strong>Pending Revenue</strong></p>
                        <small class="text-muted" style="font-size: 0.75rem;"><i class="bi bi-exclamation-triangle-fill"></i> Total revenue - payments received</small>
                    </div>
                </div>
            </div>
        </div>

        <% } else { %>
        <!-- Partner Quick Actions -->
        <div class="card border-0 shadow-sm animate-fade-in-up">
            <div class="card-body p-4">
                <h3 class="h5 mb-4"><i class="bi bi-lightning-fill text-warning"></i> Quick Access</h3>
                <div class="row g-3 quick-actions-grid">
                    <div class="col-6 col-md-4">
                        <a href="/broker" class="btn btn-outline-dark w-100 py-3">
                            <i class="bi bi-briefcase fs-4 d-block mb-2"></i>
                            <small>My Partners</small>
                        </a>
                    </div>
                    <div class="col-6 col-md-4">
                        <a href="/booking" class="btn btn-outline-primary w-100 py-3">
                            <i class="bi bi-journal-text fs-4 d-block mb-2"></i>
                            <small>Bookings</small>
                        </a>
                    </div>
                    <div class="col-6 col-md-4">
                        <a href="/payment" class="btn btn-outline-success w-100 py-3">
                            <i class="bi bi-receipt fs-4 d-block mb-2"></i>
                            <small>Payments</small>
                        </a>
                    </div>
                </div>
            </div>
        </div>
        <% } %>
        
        <% if (userRole !== 'associate' && userRole !== 'team_leader') { %>
        <!-- Quick Actions -->
        <div class="card border-0 shadow-sm animate-fade-in-up" style="animation-delay: 0.8s">
            <div class="card-body p-4">
                <h3 class="h5 mb-4"><i class="bi bi-lightning-fill text-warning"></i> Quick Actions</h3>
                <div class="row g-3 quick-actions-grid">
                    <!-- Projects Section -->
                    <div class="col-6 col-md-4 col-xl-3">
                        <a href="/project" class="btn btn-outline-info w-100 py-3">
                            <i class="bi bi-grid-3x3-gap fs-4 d-block mb-2"></i>
                            <small>Projects</small>
                        </a>
                    </div>
                    <% if (userRole === 'admin') { %>
                    <div class="col-6 col-md-4 col-xl-3">
                        <a href="/project/create" class="btn btn-outline-info w-100 py-3">
                            <i class="bi bi-plus-square fs-4 d-block mb-2"></i>
                            <small>New Project</small>
                        </a>
                    </div>
                    <% } %>
                    
                    <!-- Bookings Section -->
                    <div class="col-6 col-md-4 col-xl-3">
                        <a href="/booking" class="btn btn-outline-primary w-100 py-3">
                            <i class="bi bi-journal-text fs-4 d-block mb-2"></i>
                            <small>Bookings</small>
                        </a>
                    </div>
                    <div class="col-6 col-md-4 col-xl-3">
                        <a href="/booking/create" class="btn btn-outline-primary w-100 py-3">
                            <i class="bi bi-plus-circle fs-4 d-block mb-2"></i>
                            <small>New Booking</small>
                        </a>
                    </div>
                    
                    <!-- Payments Section -->
                    <div class="col-6 col-md-4 col-xl-3">
                        <a href="/payment" class="btn btn-outline-success w-100 py-3">
                            <i class="bi bi-receipt fs-4 d-block mb-2"></i>
                            <small>Payments</small>
                        </a>
                    </div>
                    <div class="col-6 col-md-4 col-xl-3">
                        <a href="/payment/create" class="btn btn-outline-success w-100 py-3">
                            <i class="bi bi-credit-card fs-4 d-block mb-2"></i>
                            <small>Record Payment</small>
                        </a>
                    </div>
                    
                    <!-- Customers Section -->
                    <div class="col-6 col-md-4 col-xl-3">
                        <a href="/customer" class="btn btn-outline-warning w-100 py-3">
                            <i class="bi bi-people fs-4 d-block mb-2"></i>
                            <small>Customers</small>
                        </a>
                    </div>
                    <div class="col-6 col-md-4 col-xl-3">
                        <a href="/customer/create" class="btn btn-outline-warning w-100 py-3">
                            <i class="bi bi-person-plus fs-4 d-block mb-2"></i>
                            <small>Add Customer</small>
                        </a>
                    </div>
                    
                    <!-- Partners & Teams Section -->
                    <div class="col-6 col-md-4 col-xl-3">
                        <a href="/broker" class="btn btn-outline-dark w-100 py-3">
                            <i class="bi bi-briefcase fs-4 d-block mb-2"></i>
                            <small>Partners</small>
                        </a>
                    </div>
                    <div class="col-6 col-md-4 col-xl-3">
                        <a href="/broker/create" class="btn btn-outline-dark w-100 py-3">
                            <i class="bi bi-person-plus-fill fs-4 d-block mb-2"></i>
                            <small>Add Partner</small>
                        </a>
                    </div>
                    <div class="col-6 col-md-4 col-xl-3">
                        <a href="/team" class="btn btn-outline-dark w-100 py-3">
                            <i class="bi bi-people-fill fs-4 d-block mb-2"></i>
                            <small>Teams</small>
                        </a>
                    </div>
                    <div class="col-6 col-md-4 col-xl-3">
                        <a href="/team/create" class="btn btn-outline-dark w-100 py-3">
                            <i class="bi bi-plus-circle-fill fs-4 d-block mb-2"></i>
                            <small>New Team</small>
                        </a>
                    </div>
                    
                    <!-- Employees Section -->
                    <div class="col-6 col-md-4 col-xl-3">
                        <a href="/employee" class="btn btn-outline-secondary w-100 py-3">
                            <i class="bi bi-person-workspace fs-4 d-block mb-2"></i>
                            <small>Employees</small>
                        </a>
                    </div>
                    
                    <!-- Farmer Section -->
                    <div class="col-6 col-md-4 col-xl-3">
                        <a href="/farmer" class="btn btn-outline-success w-100 py-3">
                            <i class="bi bi-flower2 fs-4 d-block mb-2"></i>
                            <small>Farmer</small>
                        </a>
                    </div>
                    
                    <!-- Admin Section -->
                    <% if (userRole === 'admin') { %>
                    <div class="col-6 col-md-4 col-xl-3">
                        <a href="/user" class="btn btn-outline-danger w-100 py-3">
                            <i class="bi bi-shield-lock fs-4 d-block mb-2"></i>
                            <small>User Management</small>
                        </a>
                    </div>
                    <% } %>
                    <div class="col-6 col-md-4 col-xl-3">
                        <a href="#" class="btn btn-outline-secondary w-100 py-3">
                            <i class="bi bi-graph-up fs-4 d-block mb-2"></i>
                            <small>Reports</small>
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Farmer QuickLinks -->
        <% if (farmerQuickLinks && farmerQuickLinks.length > 0) { %>
        <div class="card border-0 shadow-sm animate-fade-in-up mt-4" style="animation-delay: 0.9s">
            <div class="card-body p-4">
                <h3 class="h5 mb-4">
                    <i class="bi bi-flower2 text-success"></i> Farmer QuickLinks
                    <a href="/farmer/projects" class="btn btn-sm btn-outline-secondary float-end">
                        <i class="bi bi-gear"></i> Manage
                    </a>
                </h3>
                <div class="row g-3 quick-actions-grid">
                    <% farmerQuickLinks.forEach((project, index) => { %>
                    <div class="col-6 col-md-4 col-xl-3">
                        <a href="/farmer/projects/<%= project.id %>" class="btn btn-outline-success w-100 py-3">
                            <i class="bi bi-folder-fill fs-4 d-block mb-2"></i>
                            <small><%= project.name %></small>
                        </a>
                    </div>
                    <% }); %>
                </div>
            </div>
        </div>
        <% } %>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
