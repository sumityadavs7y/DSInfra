<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Partner</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
</head>
<body class="bg-light">
    <%- include('../partials/page-header', { 
        title: 'Edit Partner', 
        icon: 'bi-pencil-square',
        backUrl: '/broker/' + broker.id
    }) %>

    <div class="container my-4">
        <div class="row justify-content-center">
            <div class="col-12 col-lg-8">
                <div class="card border-0 shadow-sm">
                    <div class="card-body p-4">
                        <% if (error) { %>
                            <div class="alert alert-danger">
                                <i class="bi bi-exclamation-triangle"></i> <%= error %>
                            </div>
                        <% } %>
                        
                        <form method="POST" action="/broker/<%= broker.id %>/edit" enctype="multipart/form-data">
                            <div class="row g-3">
                                <div class="col-12 col-md-6">
                                    <label class="form-label fw-semibold">Name <span class="text-danger">*</span></label>
                                    <input type="text" class="form-control" name="name" value="<%= broker.name %>" required>
                                </div>
                                <div class="col-12 col-md-6">
                                    <label class="form-label fw-semibold">Mobile <span class="text-danger">*</span></label>
                                    <input type="tel" class="form-control" name="mobileNo" value="<%= broker.mobileNo %>" pattern="[0-9]{10,15}" required>
                                </div>
                                <div class="col-12 col-md-6">
                                    <label class="form-label fw-semibold">Email</label>
                                    <input type="email" class="form-control" name="email" value="<%= broker.email || '' %>">
                                </div>
                                <div class="col-12 col-md-6">
                                    <label class="form-label fw-semibold">Aadhaar</label>
                                    <input type="text" class="form-control" name="aadhaarNo" value="<%= broker.aadhaarNo || '' %>" pattern="[0-9]{12}" maxlength="12">
                                </div>
                                <div class="col-12 col-md-6">
                                    <label class="form-label fw-semibold">PAN</label>
                                    <input type="text" class="form-control" name="panNo" value="<%= broker.panNo || '' %>" pattern="[A-Z]{5}[0-9]{4}[A-Z]{1}" maxlength="10" style="text-transform: uppercase;">
                                </div>
                                <div class="col-12 col-md-6">
                                    <label class="form-label fw-semibold">Status</label>
                                    <select class="form-select" name="isActive">
                                        <option value="true" <%= broker.isActive ? 'selected' : '' %>>Active</option>
                                        <option value="false" <%= !broker.isActive ? 'selected' : '' %>>Inactive</option>
                                    </select>
                                </div>
                                <div class="col-12">
                                    <label class="form-label fw-semibold">Address</label>
                                    <textarea class="form-control" name="address" rows="3"><%= broker.address || '' %></textarea>
                                </div>
                                
                                <div class="col-12">
                                    <hr>
                                    <h6 class="fw-semibold mb-3"><i class="bi bi-paperclip"></i> Attachments</h6>
                                </div>
                                
                                <div class="col-12">
                                    <label class="form-label fw-semibold"><i class="bi bi-person-circle"></i> Photo</label>
                                    <% if (broker.photo) { %>
                                        <div class="mb-2">
                                            <img src="<%= broker.photo %>" alt="Current Photo" style="max-width: 150px; height: auto;" class="rounded border">
                                            <div class="form-check mt-2">
                                                <input class="form-check-input" type="checkbox" name="deletePhoto" value="true" id="deletePhoto">
                                                <label class="form-check-label text-danger" for="deletePhoto">
                                                    Delete current photo
                                                </label>
                                            </div>
                                        </div>
                                    <% } %>
                                    <input type="file" class="form-control" name="photo" accept="image/*">
                                    <small class="text-muted">Upload new photo to replace existing (max 5MB)</small>
                                </div>
                                
                                <div class="col-12">
                                    <label class="form-label fw-semibold"><i class="bi bi-file-earmark-pdf"></i> Documents</label>
                                    <% if (broker.brokerDocuments && broker.brokerDocuments.length > 0) { %>
                                        <div class="mb-2">
                                            <div class="list-group mb-2">
                                                <% broker.brokerDocuments.forEach((doc) => { %>
                                                    <div class="list-group-item d-flex justify-content-between align-items-center">
                                                        <div>
                                                            <i class="bi <%= doc.fileType === 'application/pdf' ? 'bi-file-pdf text-danger' : 'bi-file-image text-primary' %>"></i>
                                                            <%= doc.fileName %>
                                                            <small class="text-muted">(<%= (doc.fileSize / 1024).toFixed(1) %> KB)</small>
                                                        </div>
                                                        <div class="form-check">
                                                            <input class="form-check-input" type="checkbox" name="deleteDocuments" value="<%= doc.id %>" id="deleteDoc<%= doc.id %>">
                                                            <label class="form-check-label text-danger small" for="deleteDoc<%= doc.id %>">
                                                                Delete
                                                            </label>
                                                        </div>
                                                    </div>
                                                <% }) %>
                                            </div>
                                        </div>
                                    <% } %>
                                    <input type="file" class="form-control" name="documents" accept="image/*,.pdf" multiple>
                                    <small class="text-muted">Upload additional documents (max 5MB each)</small>
                                </div>
                            </div>
                            
                            <div class="mt-4 d-flex gap-2 justify-content-end">
                                <a href="/broker/<%= broker.id %>" class="btn btn-secondary">Cancel</a>
                                <button type="submit" class="btn btn-dark">
                                    <i class="bi bi-check-circle"></i> Update Partner
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

