<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Associate</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
</head>
<body class="bg-light">
    <nav class="navbar navbar-dark bg-dark shadow-sm">
        <div class="container-fluid">
            <div class="d-flex align-items-center">
                <a href="/broker/<%= broker.id %>" class="btn btn-outline-light btn-sm me-2">
                    <i class="bi bi-arrow-left"></i> Back
                </a>
                <img src="/images/logo.png" alt="Logo" style="height: 30px; width: auto; margin-right: 10px;">
                <span class="navbar-brand mb-0 h1">Edit Associate</span>
            </div>
        </div>
    </nav>

    <div class="container my-4">
        <div class="row justify-content-center">
            <div class="col-12 col-lg-8">
                <div class="card border-0 shadow-sm">
                    <div class="card-body p-4">
                        <% if (error) { %>
                            <div class="alert alert-danger">
                                <i class="bi bi-exclamation-triangle"></i> <%= error %>
                            </div>
                        <% } %>
                        
                        <form method="POST" action="/broker/<%= broker.id %>/edit">
                            <div class="row g-3">
                                <div class="col-12 col-md-6">
                                    <label class="form-label fw-semibold">Name <span class="text-danger">*</span></label>
                                    <input type="text" class="form-control" name="name" value="<%= broker.name %>" required>
                                </div>
                                <div class="col-12 col-md-6">
                                    <label class="form-label fw-semibold">Mobile <span class="text-danger">*</span></label>
                                    <input type="tel" class="form-control" name="mobileNo" value="<%= broker.mobileNo %>" pattern="[0-9]{10,15}" required>
                                </div>
                                <div class="col-12 col-md-6">
                                    <label class="form-label fw-semibold">Email</label>
                                    <input type="email" class="form-control" name="email" value="<%= broker.email || '' %>">
                                </div>
                                <div class="col-12 col-md-6">
                                    <label class="form-label fw-semibold">Aadhaar</label>
                                    <input type="text" class="form-control" name="aadhaarNo" value="<%= broker.aadhaarNo || '' %>" pattern="[0-9]{12}" maxlength="12">
                                </div>
                                <div class="col-12 col-md-6">
                                    <label class="form-label fw-semibold">PAN</label>
                                    <input type="text" class="form-control" name="panNo" value="<%= broker.panNo || '' %>" pattern="[A-Z]{5}[0-9]{4}[A-Z]{1}" maxlength="10" style="text-transform: uppercase;">
                                </div>
                                <div class="col-12 col-md-6">
                                    <label class="form-label fw-semibold">Status</label>
                                    <select class="form-select" name="isActive">
                                        <option value="true" <%= broker.isActive ? 'selected' : '' %>>Active</option>
                                        <option value="false" <%= !broker.isActive ? 'selected' : '' %>>Inactive</option>
                                    </select>
                                </div>
                                <div class="col-12">
                                    <label class="form-label fw-semibold">Address</label>
                                    <textarea class="form-control" name="address" rows="3"><%= broker.address || '' %></textarea>
                                </div>
                            </div>
                            
                            <div class="mt-4 d-flex gap-2 justify-content-end">
                                <a href="/broker/<%= broker.id %>" class="btn btn-secondary">Cancel</a>
                                <button type="submit" class="btn btn-dark">
                                    <i class="bi bi-check-circle"></i> Update Associate
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

