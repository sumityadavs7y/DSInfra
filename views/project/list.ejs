<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Projects</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
</head>
<body class="bg-light">
        <%- include('../partials/page-header', { 
        title: 'Projects', 
        icon: 'bi-building', 
        backUrl: '/dashboard', 
        actionUrl: '/project/create', 
        actionText: 'New Project', 
        actionIcon: 'bi-plus-circle'
    }) %>

    <div class="container my-4">
        <!-- Show/Hide Deleted Toggle -->
        <div class="mb-3">
            <% if (showDeleted) { %>
                <a href="/project" class="btn btn-outline-danger">
                    <i class="bi bi-eye-slash"></i> Hide Deleted
                </a>
            <% } else { %>
                <a href="/project?showDeleted=true" class="btn btn-outline-secondary">
                    <i class="bi bi-trash"></i> Show Deleted
                </a>
            <% } %>
        </div>

        <!-- Search & Filter -->
        <div class="card border-0 shadow-sm mb-4">
            <div class="card-body">
                <form action="/project" method="GET" class="row g-3">
                    <input type="hidden" name="showDeleted" value="<%= showDeleted ? 'true' : '' %>">
                    <div class="col-12 col-md-6">
                        <input type="text" class="form-control" placeholder="Search by project name or location..." 
                               name="search" value="<%= search || '' %>">
                    </div>
                    <div class="col-12 col-md-3">
                        <select class="form-select" name="status">
                            <option value="">All Status</option>
                            <option value="active" <%= status === 'active' ? 'selected' : '' %>>Active</option>
                            <option value="inactive" <%= status === 'inactive' ? 'selected' : '' %>>Inactive</option>
                        </select>
                    </div>
                    <div class="col-12 col-md-3">
                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary flex-fill">
                                <i class="bi bi-search"></i> Search
                            </button>
                            <% if (search || status) { %>
                            <a href="/project" class="btn btn-outline-secondary">
                                <i class="bi bi-x-circle"></i>
                            </a>
                            <% } %>
                        </div>
                    </div>
                </form>
            </div>
        </div>

        <!-- Projects Grid -->
        <% if (projects.length === 0) { %>
        <div class="card border-0 shadow-sm">
            <div class="card-body text-center py-5">
                <i class="bi bi-building fs-1 text-muted"></i>
                <p class="text-muted mt-3">No projects found</p>
                <% if (userRole === 'admin') { %>
                <a href="/project/create" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i> Create First Project
                </a>
                <% } %>
            </div>
        </div>
        <% } else { %>
        <div class="row g-4">
            <% projects.forEach(project => { %>
            <div class="col-12 col-md-6 col-lg-4">
                <div class="card h-100 border-0 shadow-sm <%= !project.isActive || project.isDeleted ? 'opacity-75' : '' %> <%= project.isDeleted ? 'border-danger border-2' : '' %>">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-start mb-3">
                            <h5 class="card-title text-info mb-0">
                                <i class="bi bi-building"></i>
                                <%= project.projectName %>
                            </h5>
                            <div>
                                <% if (project.isDeleted) { %>
                                <span class="badge bg-danger me-1">Deleted</span>
                                <% } %>
                                <span class="badge bg-<%= project.isActive ? 'success' : 'secondary' %>">
                                    <%= project.isActive ? 'Active' : 'Inactive' %>
                                </span>
                            </div>
                        </div>
                        
                        <p class="text-muted mb-2">
                            <i class="bi bi-geo-alt"></i> <%= project.location %>
                        </p>
                        
                        <% if (project.description) { %>
                        <p class="card-text small text-muted mb-3">
                            <%= project.description.substring(0, 100) %><%= project.description.length > 100 ? '...' : '' %>
                        </p>
                        <% } %>
                        
                        <hr>
                        
                        <div class="row text-center g-2 mb-3">
                            <div class="col-6">
                                <small class="text-muted d-block">Total Plots</small>
                                <strong class="h5 mb-0"><%= project.totalPlots %></strong>
                            </div>
                            <div class="col-6">
                                <small class="text-muted d-block">Available</small>
                                <strong class="h5 mb-0 text-<%= project.availablePlots > 0 ? 'success' : 'danger' %>">
                                    <%= project.availablePlots %>
                                </strong>
                            </div>
                        </div>
                        
                        <div class="d-flex gap-2">
                            <a href="/project/<%= project.id %>" class="btn btn-sm btn-info flex-fill">
                                <i class="bi bi-eye"></i> View
                            </a>
                            <% if (userRole === 'admin' && !project.isDeleted) { %>
                            <a href="/project/<%= project.id %>/edit" class="btn btn-sm btn-warning">
                                <i class="bi bi-pencil"></i>
                            </a>
                            <form method="POST" action="/project/<%= project.id %>/delete" style="display:inline;" 
                                  onsubmit="return confirm('Delete this project? This will also delete all related bookings and payments.');">
                                <button type="submit" class="btn btn-sm btn-danger">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </form>
                            <% } %>
                        </div>
                    </div>
                    <div class="card-footer bg-light text-muted small">
                        <i class="bi bi-calendar"></i> 
                        <%= new Date(project.createdAt).toLocaleDateString('en-IN') %>
                    </div>
                </div>
            </div>
            <% }) %>
        </div>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

