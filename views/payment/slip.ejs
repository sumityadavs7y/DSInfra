<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Receipt - <%= payment.receiptNo %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        @media print {
            .no-print {
                display: none !important;
            }
            body {
                margin: 0;
                padding: 0;
            }
            .print-container {
                box-shadow: none !important;
                margin: 0;
                padding: 15px 20px;
            }
            .header-section {
                display: none !important;
            }
        }
        body {
            background-color: #f5f5f5;
            font-family: 'Arial', sans-serif;
            font-size: 12px;
        }
        .print-container {
            max-width: 800px;
            margin: 10px auto;
            background: white;
            padding: 30px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        .header-section {
            text-align: center;
            border-bottom: 2px solid #198754;
            padding-bottom: 12px;
            margin-bottom: 15px;
        }
        .company-name {
            font-size: 20px;
            font-weight: bold;
            color: #198754;
            margin-bottom: 3px;
        }
        .document-title {
            font-size: 16px;
            font-weight: bold;
            color: #333;
            margin-top: 8px;
            text-decoration: underline;
        }
        .info-row {
            display: flex;
            padding: 4px 0;
            border-bottom: 1px solid #eee;
            font-size: 11px;
        }
        .info-label {
            font-weight: bold;
            min-width: 140px;
            color: #555;
        }
        .info-value {
            flex: 1;
            color: #333;
        }
        .section-title {
            font-size: 13px;
            font-weight: bold;
            color: #198754;
            margin-top: 12px;
            margin-bottom: 8px;
            padding-bottom: 3px;
            border-bottom: 1.5px solid #198754;
        }
        .amount-box {
            background-color: #d1e7dd;
            border: 1.5px solid #198754;
            padding: 12px;
            margin: 12px 0;
            text-align: center;
            border-radius: 5px;
        }
        .amount-highlight {
            font-size: 22px;
            font-weight: bold;
            color: #198754;
        }
        .amount-words {
            font-size: 11px;
            font-style: italic;
            color: #555;
            margin-top: 5px;
        }
        .footer-section {
            margin-top: 15px;
            padding-top: 10px;
            border-top: 1px solid #ddd;
            font-size: 10px;
        }
        .signature-box {
            margin-top: 25px;
            text-align: center;
        }
        .signature-line {
            border-top: 1px solid #333;
            width: 180px;
            margin: 0 auto;
            padding-top: 3px;
            font-size: 10px;
        }
        .print-button {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 1000;
        }
        .stamp-box {
            margin-top: 20px;
            text-align: right;
            font-size: 10px;
        }
        .note-box {
            margin-top: 15px;
            padding: 8px;
            background-color: #fff3cd;
            border-left: 3px solid #ffc107;
            border-radius: 3px;
            font-size: 10px;
        }
        @page {
            margin: 25mm 15mm 20mm 15mm;
            size: A4;
        }
    </style>
</head>
<body>
    <!-- Print Button -->
    <button onclick="window.print()" class="btn btn-success no-print print-button">
        <i class="bi bi-printer"></i> Print
    </button>

    <div class="print-container">
        <!-- Header -->
        <div class="header-section">
            <div class="company-name">DS INFRA DEVELOPERS</div>
            <div style="font-size: 14px; color: #666;">Lucknow</div>
            <div class="document-title">PAYMENT RECEIPT</div>
        </div>

        <!-- Receipt Information -->
        <div class="info-row">
            <div class="info-label">Receipt No:</div>
            <div class="info-value"><strong><%= payment.receiptNo %></strong></div>
        </div>
        <div class="info-row">
            <div class="info-label">Receipt Date:</div>
            <div class="info-value">
                <%= new Date(payment.receiptDate).toLocaleDateString('en-IN', { 
                    year: 'numeric', month: 'long', day: 'numeric'
                }) %>
            </div>
        </div>
        <div class="info-row">
            <div class="info-label">Payment Mode:</div>
            <div class="info-value"><strong><%= payment.paymentType %></strong></div>
        </div>
        <% if (payment.chequeNo) { %>
        <div class="info-row">
            <div class="info-label">Cheque/Transaction No:</div>
            <div class="info-value"><%= payment.chequeNo %></div>
        </div>
        <% } %>
        <% if (payment.bankName) { %>
        <div class="info-row">
            <div class="info-label">Bank Name:</div>
            <div class="info-value"><%= payment.bankName %></div>
        </div>
        <% } %>

        <!-- Amount Box -->
        <div class="amount-box">
            <div style="font-size: 16px; color: #555; margin-bottom: 10px;">Amount Received</div>
            <div class="amount-highlight">₹<%= parseFloat(payment.paymentAmount).toLocaleString('en-IN') %></div>
            <div class="amount-words">
                (Rupees <%= amountInWords %> Only)
            </div>
        </div>

        <!-- Customer Details -->
        <div class="section-title">Received From</div>
        <div class="info-row">
            <div class="info-label">Customer No:</div>
            <div class="info-value"><%= payment.booking.customer.customerNo %></div>
        </div>
        <div class="info-row">
            <div class="info-label">Name:</div>
            <div class="info-value"><strong><%= payment.booking.customer.applicantName %></strong></div>
        </div>
        <div class="info-row">
            <div class="info-label">Father/Husband Name:</div>
            <div class="info-value"><%= payment.booking.customer.fatherOrHusbandName %></div>
        </div>
        <div class="info-row">
            <div class="info-label">Mobile No:</div>
            <div class="info-value"><%= payment.booking.customer.mobileNo %></div>
        </div>
        <div class="info-row">
            <div class="info-label">Address:</div>
            <div class="info-value"><%= payment.booking.customer.address %></div>
        </div>

        <!-- Booking Details -->
        <div class="section-title">Payment Against Booking</div>
        <div class="info-row">
            <div class="info-label">Booking No:</div>
            <div class="info-value"><strong><%= payment.booking.bookingNo %></strong></div>
        </div>
        <div class="info-row">
            <div class="info-label">Project:</div>
            <div class="info-value"><%= payment.booking.project.projectName %></div>
        </div>
        <div class="info-row">
            <div class="info-label">Plot No:</div>
            <div class="info-value"><strong style="font-size: 18px; color: #198754;"><%= payment.booking.plotNo %></strong></div>
        </div>
        <div class="info-row">
            <div class="info-label">Total Booking Amount:</div>
            <div class="info-value">₹<%= parseFloat(payment.booking.totalAmount).toLocaleString('en-IN') %></div>
        </div>

        <% if (payment.remarks) { %>
        <!-- Remarks -->
        <div class="section-title">Remarks</div>
        <div style="padding: 10px; background-color: #f8f9fa; border-radius: 5px;">
            <%= payment.remarks %>
        </div>
        <% } %>

        <!-- Footer -->
        <div class="footer-section">
            <div class="row">
                <div class="col-6">
                    <small class="text-muted">
                        Generated on: <%= new Date().toLocaleString('en-IN') %><br>
                        Generated by: <%= userName %>
                    </small>
                </div>
                <div class="col-6">
                    <div class="stamp-box">
                        <div style="margin-bottom: 40px; font-weight: bold; color: #999;">
                            [Company Seal]
                        </div>
                        <div class="signature-line">Authorized Signature</div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Important Note -->
        <div class="note-box">
            <strong>Note:</strong> This is a computer-generated receipt. Please retain for future reference.
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

